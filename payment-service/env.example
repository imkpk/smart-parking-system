# Copy to your shell before starting payment-service, or export in your IDE run config.
# Must exactly match JWT_SECRET in backend/.env and be at least 32 characters.
JWT_SECRET=smart_parking_dev_jwt_secret_32_chars_minimum