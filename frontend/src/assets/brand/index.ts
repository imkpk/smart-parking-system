import authHeroDashboard from './auth-hero-dashboard.png';

/** Bundled brand imagery — add new assets here and in the design-system license audit. */
export const brandAssets = {
  authHeroDashboard,
} as const;